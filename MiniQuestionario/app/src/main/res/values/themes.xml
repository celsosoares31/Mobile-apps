<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiniQuestionario" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
        <item name="backgroundTint">@color/lightBlue</item>
        <item name="background">@color/lightBlue</item>
    </style>

    <style name="Theme.MiniQuestionario" parent="Base.Theme.MiniQuestionario" />
</resources>